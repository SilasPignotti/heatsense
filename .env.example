# HeatSense Environment Variables Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# Google Earth Engine Configuration
# =============================================================================
# Your Google Earth Engine project ID (required for satellite data analysis)
# Get this from: https://console.cloud.google.com/
UHI_EARTH_ENGINE_PROJECT=your-gee-project-id

# =============================================================================
# Flask Web Application Configuration
# =============================================================================
# Secret key for Flask session management (required for web interface)
# Generate a strong secret key for production: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=development-only-change-in-production

# Flask environment (development/production)
FLASK_ENV=development

# =============================================================================
# Optional Configuration
# =============================================================================
# Log level for application logging (DEBUG, INFO, WARNING, ERROR)
UHI_LOG_LEVEL=INFO

# Custom data directories (optional)
# UHI_DATA_DIR=/path/to/your/data
# UHI_CACHE_DIR=/path/to/your/cache

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Set UHI_EARTH_ENGINE_PROJECT to your Google Earth Engine project ID
# 3. Generate and set a strong SECRET_KEY for production use
# 4. Authenticate with Google Earth Engine: uv run python -c "import ee; ee.Authenticate()"
# 5. Run the application: uv run python run_webapp.py